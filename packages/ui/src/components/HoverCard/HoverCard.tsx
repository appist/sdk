import { component$, HTMLAttributes } from "@builder.io/qwik";

export interface HoverCardProps extends HTMLAttributes<HTMLElement> {}

export const HoverCard = component$((props: HoverCardProps) => {
	return <></>;
});
